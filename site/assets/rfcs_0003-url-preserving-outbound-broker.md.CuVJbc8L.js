import{_ as a,o as t,c as n,a2 as r}from"./chunks/framework.DYQ4mM3C.js";const h=JSON.parse('{"title":"RFC 0003: URL-preserving outbound broker","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0003-url-preserving-outbound-broker.md","filePath":"rfcs/0003-url-preserving-outbound-broker.md","lastUpdated":null}'),i={name:"rfcs/0003-url-preserving-outbound-broker.md"};function o(s,e,l,c,d,p){return t(),n("div",null,[...e[0]||(e[0]=[r(`<h1 id="rfc-0003-url-preserving-outbound-broker" tabindex="-1">RFC 0003: URL-preserving outbound broker <a class="header-anchor" href="#rfc-0003-url-preserving-outbound-broker" aria-label="Permalink to &quot;RFC 0003: URL-preserving outbound broker&quot;">​</a></h1><ul><li>Status: Implemented experimentally</li><li>Date: 2026-08-24</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>EnvVault can attach an existing provider-proxy profile to a sandbox without changing the provider URL used by the application. A host-side outbound broker intercepts only configured HTTP or HTTPS destinations, applies the profile&#39;s method and path policy, validates a late-bound <code>envvault://</code> credential reference in the configured bearer field, and injects the upstream credential. The sandbox receives the non-secret reference, a short-lived proxy capability, and an ephemeral public CA certificate, never the upstream credential or CA private key.</p><p>The first runtime adapter targets Docker and Node-based coding agents. It sets standard proxy environment variables, enables Node&#39;s environment-proxy support, and mounts the public CA read-only. This is cooperative interception, not network enforcement, so the execution continues to report <code>brokered</code> rather than <code>brokered-enforced</code>.</p><h2 id="motivation" tabindex="-1">Motivation <a class="header-anchor" href="#motivation" aria-label="Permalink to &quot;Motivation&quot;">​</a></h2><p>The original provider proxy requires an application-specific base URL change. That works well for SDKs with a supported <code>base_url</code> option, including the current Codex adapter, but does not compose transparently with every tool that an agent may launch. URL-preserving outbound attachment moves this concern to a runtime adapter:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sandbox application</span></span>
<span class="line"><span>  original provider URL</span></span>
<span class="line"><span>  normal API-key variable = envvault://&lt;credential&gt;</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        | HTTP(S)_PROXY + short-lived capability</span></span>
<span class="line"><span>        v</span></span>
<span class="line"><span>trusted EnvVault outbound broker</span></span>
<span class="line"><span>  destination + method + path policy</span></span>
<span class="line"><span>  ephemeral TLS interception CA</span></span>
<span class="line"><span>  upstream credential injection</span></span>
<span class="line"><span>        |</span></span>
<span class="line"><span>        v</span></span>
<span class="line"><span>configured provider destination</span></span></code></pre></div><p>The application may continue to address the original host and initialize its normal SDK from its normal environment-variable name. The value is the exact underlying credential reference used by the attached profile, not a raw secret or an arbitrary placeholder. The SDK sends that reference in its ordinary bearer field, and the broker resolves it at the last trusted hop.</p><h2 id="runtime-neutral-contracts" tabindex="-1">Runtime-neutral contracts <a class="header-anchor" href="#runtime-neutral-contracts" aria-label="Permalink to &quot;Runtime-neutral contracts&quot;">​</a></h2><p>The connection layer owns <code>EgressBroker</code>, <code>EgressLease</code>, <code>EgressRoute</code>, and <code>EgressClientConfig</code>:</p><ul><li>a route combines one validated connection policy with non-secret grant metadata;</li><li>the broker acquires credentials through <code>CredentialProvider</code> and keeps them in the trusted process;</li><li>a lease exposes only a proxy endpoint, expiry, redacted client configuration, and an idempotent close operation; and</li><li>the client configuration contains a capability-bearing proxy URL, public CA certificate, and the non-secret credential references approved for late binding.</li></ul><p>The sandbox layer owns <code>EgressAttacher</code>. A runtime translates the secretless client configuration into environment, mounts, and cleanup resources. This keeps Docker-specific trust delivery out of the connection broker and permits future container, external-sandbox, and microVM adapters.</p><h2 id="docker-attachment" tabindex="-1">Docker attachment <a class="header-anchor" href="#docker-attachment" aria-label="Permalink to &quot;Docker attachment&quot;">​</a></h2><p><code>envvault sandbox run --outbound-profile &lt;profile&gt;</code> opens one outbound lease for all selected profiles. The flag is repeatable. <code>--all</code> is a CLI selection shortcut that expands every provider-proxy profile whose project binding permits the current workspace; it does not bypass policy checks or change the runtime contract. Docker receives:</p><ul><li><code>HTTP_PROXY</code>, <code>HTTPS_PROXY</code>, and their lowercase forms, containing a random short-lived proxy capability;</li><li><code>NODE_USE_ENV_PROXY=1</code>;</li><li><code>NODE_EXTRA_CA_CERTS=/etc/envvault-outbound/ca.pem</code>; and</li><li>a read-only mount containing only the ephemeral public CA certificate.</li></ul><p>Direct references from <code>--env</code> or <code>--env-file</code> remain literal only when they exactly match one of the selected profiles&#39; underlying credentials. An unmatched direct reference fails before the container is created unless the operator explicitly selects the raw-materialization compatibility mode.</p><p>The example Codex image uses Node 22.22.2. Node&#39;s <a href="https://nodejs.org/api/http.html#built-in-proxy-support" target="_blank" rel="noreferrer">built-in environment proxy support</a> is available from Node 22.21.0 and 24.5.0. Other language runtimes may honor the proxy variables but need their own additive trust-store adapter.</p><p>When the invocation also uses EnvVault&#39;s existing base-URL gateway, its exact host and port are added to <code>NO_PROXY</code>. This avoids recursively sending one EnvVault broker through the other.</p><h2 id="broker-behavior" tabindex="-1">Broker behavior <a class="header-anchor" href="#broker-behavior" aria-label="Permalink to &quot;Broker behavior&quot;">​</a></h2><p>The broker requires HTTP Basic proxy authentication using the random lease capability. It supports absolute-form HTTP requests and <code>CONNECT</code>. Node may use <code>CONNECT</code> for both HTTP and HTTPS origins, so configured destinations are parsed and authorized inside either kind of tunnel.</p><p>For a configured route, the broker:</p><ol><li>requires the configured authentication field to contain <code>Bearer</code> plus the route&#39;s exact <code>envvault://&lt;credential&gt;</code> reference;</li><li>fixes the upstream scheme, host, port, and base path from trusted policy;</li><li>rejects encoded, traversal-bearing, disallowed-method, and disallowed-path requests;</li><li>removes hop-by-hop and proxy authentication headers;</li><li>replaces only the configured authentication header with the upstream bearer credential; and</li><li>refuses to follow upstream redirects while holding that credential.</li></ol><p>Missing references, references for another credential, and raw values are rejected locally and never reach the configured upstream. The broker does not scan or rewrite request bodies, query strings, or arbitrary header values.</p><p>HTTPS routes use a per-lease ECDSA CA. Its private key remains in broker memory; leaf certificates are minted only for configured destinations and last no longer than the lease. The broker currently serves HTTP/1.1 inside intercepted tunnels.</p><p>Requests to unconfigured public HTTP or HTTPS destinations may pass through without credential injection. The broker refuses to tunnel an unconfigured loopback, private, link-local, or unspecified destination so the host-side proxy does not become an obvious private-network relay. This check does not replace runtime egress enforcement: the Docker container still has direct bridge networking and can bypass the proxy.</p><h2 id="credential-and-tls-boundary" tabindex="-1">Credential and TLS boundary <a class="header-anchor" href="#credential-and-tls-boundary" aria-label="Permalink to &quot;Credential and TLS boundary&quot;">​</a></h2><p>The upstream credential may exist only in the credential provider, broker memory, and authenticated upstream request. It is excluded from sandbox environment, mounts, command arguments, client configuration, and CLI output. The <code>envvault://</code> reference is an identifier rather than secret material and may appear in <code>.env</code>, process environment, and the outgoing pre-broker request.</p><p>The proxy capability is intentionally available in the sandbox environment and Docker metadata. Anyone able to read it can use the configured broker routes until lease expiry or cleanup. The CA certificate is public; its private key is not mounted. The trusted broker can observe allowed request metadata and bodies because TLS terminates there. TLS certificate pinning and clients that ignore the mounted CA are incompatible with this prototype.</p><h2 id="agent-authentication-composition" tabindex="-1">Agent authentication composition <a class="header-anchor" href="#agent-authentication-composition" aria-label="Permalink to &quot;Agent authentication composition&quot;">​</a></h2><p>Model OAuth remains owned by each coding agent. For Codex, <code>--agent-auth native</code> mounts one explicit EnvVault-managed <code>CODEX_HOME</code>; OAuth login, refresh, and logout remain Codex behavior. Unconfigured public OAuth and model endpoints are tunneled without EnvVault credential injection.</p><p>Provider-specific agent state remains behind agent adapters. Outbound profiles are independent connection routes for tools the agent invokes. The current legacy provider-profile translator supports late binding in bearer authentication only. Provider-specific API-key headers, signed requests, OAuth token exchange, and Kaggle/Gemini-specific behavior require separate protocol/provider adapters.</p><h2 id="failure-and-cleanup" tabindex="-1">Failure and cleanup <a class="header-anchor" href="#failure-and-cleanup" aria-label="Permalink to &quot;Failure and cleanup&quot;">​</a></h2><ul><li>An unsupported runtime, invalid profile, project-binding mismatch, duplicate route, missing credential, reserved proxy environment variable, or mount conflict fails before the container is created.</li><li>A direct reference not covered by a selected outbound profile fails before the container is created; a missing or mismatched bearer reference on a configured request fails at the broker without contacting the provider.</li><li>A partial broker start revokes every acquired credential lease and clears owned credential buffers.</li><li>Container completion, lifecycle failure, cancellation, or interrupt closes the broker, active tunnels, credential leases, and temporary CA directory.</li><li>Capabilities and client configuration implement redacted formatting.</li></ul><h2 id="limitations-and-next-steps" tabindex="-1">Limitations and next steps <a class="header-anchor" href="#limitations-and-next-steps" aria-label="Permalink to &quot;Limitations and next steps&quot;">​</a></h2><p>This slice does not provide transparent kernel interception, default-deny egress, DNS policy, system-wide CA installation, certificate-pinning support, HTTP/2 interception, configured-destination WebSocket upgrades, byte/connection limit enforcement, or audit events. It does not claim to be a general TLS inspection appliance.</p><p>The enforcement follow-up should put routing outside the untrusted workload:</p><ol><li>a Docker sidecar or host service with TPROXY/nftables rules and a private network;</li><li>equivalent routing in a microVM TAP/host-firewall adapter;</li><li>default-deny DNS and IP egress, including metadata and management networks;</li><li>tests proving that direct IP, alternate DNS, proxy bypass, and expired capabilities fail closed; and</li><li>metadata-only allow/deny audit events.</li></ol><p>Only a runtime that passes those checks may raise the security level to <code>brokered-enforced</code>.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Unit tests exercise HTTPS interception, original-URL preservation, proxy authentication, exact late-bound reference validation, missing and mismatched reference rejection, method/path rejection, credential replacement, redaction, and lease shutdown. Opt-in Docker tests exercise Node&#39;s real proxy behavior for HTTP and HTTPS, the mounted CA, an SDK-style credential environment variable, upstream credential injection, absence of the credential from sandbox metadata, and container/broker cleanup.</p>`,41)])])}const m=a(i,[["render",o]]);export{h as __pageData,m as default};
