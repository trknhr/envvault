import{_ as t,o as a,c as i,a2 as r}from"./chunks/framework.DYQ4mM3C.js";const u=JSON.parse('{"title":"Threat Model","description":"","frontmatter":{},"headers":[],"relativePath":"threat-model.md","filePath":"threat-model.md","lastUpdated":1784876061000}'),o={name:"threat-model.md"};function n(s,e,l,d,c,p){return a(),i("div",null,[...e[0]||(e[0]=[r('<h1 id="threat-model" tabindex="-1">Threat Model <a class="header-anchor" href="#threat-model" aria-label="Permalink to &quot;Threat Model&quot;">​</a></h1><p>EnvVault protects long-lived local credential material from routine project and <code>.env</code> exposure. It does not defend against a fully compromised OS user account.</p><h2 id="assets" tabindex="-1">Assets <a class="header-anchor" href="#assets" aria-label="Permalink to &quot;Assets&quot;">​</a></h2><ul><li>Credential values stored in the OS credential store.</li><li>Credential files inside temporary isolated-home workspaces while a child process is running.</li><li>Local proxy bearer tokens while the child process is running.</li><li>URL-preserving outbound proxy capabilities and ephemeral CA private keys while a sandbox lease is active.</li><li>External sandbox plugin capabilities while a sandbox lease is active.</li><li>Persistent native agent OAuth state stored under EnvVault&#39;s private data directory.</li><li>EnvVault config policy and project-binding approvals.</li></ul><h2 id="trust-boundaries" tabindex="-1">Trust Boundaries <a class="header-anchor" href="#trust-boundaries" aria-label="Permalink to &quot;Trust Boundaries&quot;">​</a></h2><ul><li>OS credential store: stores long-lived raw secrets.</li><li>EnvVault config directory: stores non-secret settings and optional proxy policy.</li><li>Child process environment: receives resolved credential values or local proxy bearer tokens.</li><li>Isolated home workspace: receives credential values requested with <code>--home-file</code> and becomes the child process&#39;s home without copying or writing the requested destination under the real home directory.</li><li>Localhost provider proxy: accepts local proxy bearer tokens and adds provider API keys for allowlisted requests.</li><li>Host-side outbound broker: terminates configured HTTP/HTTPS routes, enforces destination/method/path policy, and adds provider credentials while keeping its ephemeral CA private key outside the sandbox.</li><li>Experimental Docker sandbox: receives a short-lived gateway capability but not the upstream credential in brokered mode.</li><li>Native agent auth mount: an explicitly selected, writable, profile-specific agent home containing OAuth tokens and persistent agent configuration.</li><li>External sandbox control plane: trusted to own the plugin stdin/stdout, inject selected capability values, apply egress policy, and revoke leases.</li><li>External agent sandbox: untrusted; receives selected gateway capabilities but never the plugin control channel or upstream credential.</li><li>Docker daemon: trusted host component that creates, starts, stops, and removes the application container.</li><li>Third-party provider API: receives the real provider API key from the child process or proxy.</li></ul><h2 id="in-scope" tabindex="-1">In Scope <a class="header-anchor" href="#in-scope" aria-label="Permalink to &quot;In Scope&quot;">​</a></h2><ul><li>Accidental commit of <code>.env</code> files containing <code>envvault://</code> references.</li><li>Third-party SDKs that expect normal API key environment variables.</li><li>Third-party tools that require credential files at fixed paths below their home directory.</li><li>Third-party SDKs that can be configured with both a custom base URL and bearer token.</li><li>Proxy-aware sandbox applications that must retain the provider&#39;s original URL and can send a request without possessing the real bearer credential.</li><li>Repository changes that try to request a different credential, proxy target URL, method, or path.</li><li>Child processes that can inspect their own environment.</li><li>Application containers that inspect their environment, image metadata, and mounted workspace.</li><li>Application containers explicitly granted a native agent auth profile that inspect or modify that persistent profile.</li><li>Existing agent sandboxes integrated through a trusted host-side plugin.</li></ul><h2 id="out-of-scope" tabindex="-1">Out of Scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to &quot;Out of Scope&quot;">​</a></h2><ul><li>Malicious code running as the same OS user with arbitrary process and keychain access.</li><li>Kernel, hypervisor, firmware, or hardware compromise.</li><li>Browser compromise or malicious browser extensions.</li><li>Application-level data written using a valid credential.</li><li>DLP for prompts, stdout, stderr, HTTP request bodies, or third-party application logs.</li><li>Keeping a credential out of the child process when an SDK or tool requires the raw credential directly and cannot be pointed at the EnvVault localhost proxy.</li><li>Protecting native agent OAuth state from a container after the operator explicitly selects <code>--agent-auth native</code>.</li><li>Default-deny Docker egress, DNS enforcement, cloud metadata blocking, and isolation from every host service in experimental <code>brokered</code> mode.</li><li>Transparent interception for clients that ignore proxy settings, use TLS certificate pinning, or require an unsupported language-specific trust store.</li><li>Docker daemon compromise or container escape.</li><li>A malicious or compromised external sandbox control plane. It owns the temporary capabilities and is part of the trusted computing base.</li><li>Default-deny enforcement by a third-party sandbox platform. The generic plugin reports only <code>brokered</code>.</li></ul><h2 id="security-controls" tabindex="-1">Security Controls <a class="header-anchor" href="#security-controls" aria-label="Permalink to &quot;Security Controls&quot;">​</a></h2><ul><li>Credential values are stored in the OS credential store.</li><li><code>envvault inspect</code> scans local file contents in-process without Git history, network verification, or symlink traversal. Its output model contains only path, location, rule, and confidence metadata; matched values are discarded.</li><li>Direct references use strict <code>envvault://&lt;credential&gt;</code> parsing.</li><li><code>.env</code> references reject query strings, fragments, path traversal, and percent-encoded separators.</li><li><code>--home-file</code> accepts only safe relative destination paths and creates a private, otherwise empty home. Template sources may be relative to the invocation working directory or absolute; source files must be regular files and may not be symlinks.</li><li>Home-file JSON, YAML, and TOML resolution replaces only whole-string direct credential references in values, never keys. It rejects embedded or proxy references, ambiguous input, non-string YAML mapping keys, and unsupported YAML anchors, aliases, and merge keys; credentials are safely encoded as strings and results are written with user-only permissions. Source templates are not modified or written back.</li><li>Isolated-home workspaces are removed after normal child exit. Stale workspaces left by forced termination or system failure are removed by <code>envvault doctor --repair</code>.</li><li>Optional proxy policy fixes target URL, allowed HTTP methods, allowed paths, and local token lifetime.</li><li>Proxy <code>.env</code> references split proxy base URLs from local-only bearer tokens.</li><li>Non-interactive unapproved project bindings fail closed for proxy use.</li><li><code>sandbox run --all</code> rechecks project bindings, selects only provider-proxy profiles allowed for the current project, and fails when none are eligible. Because newly added eligible profiles expand its authority automatically, explicit profile names remain the least-authority, reproducible choice.</li><li>Audit records are metadata-only.</li><li>Docker sandbox mode rejects direct credential references unless the exact reference belongs to an attached outbound profile or <code>--allow-materialized-secrets</code> is explicit. Proxy mode places only the local gateway URL and short-lived token in the container environment and metadata.</li><li>URL-preserving outbound mode places a non-secret, late-bound credential reference, a short-lived authenticated proxy URL, and an ephemeral public CA in the sandbox. The broker requires the exact reference in the configured bearer field, fixes configured destinations, checks exact methods and paths, removes hop-by-hop headers, replaces only that bearer field, and does not follow credential-bearing redirects. Its CA private key and provider credential remain host-side.</li><li>The outbound broker refuses unconfigured private, loopback, link-local, and unspecified destinations. This prevents the broker itself from becoming a general private-network relay; it does not block direct container traffic.</li><li>Native agent auth is explicit, uses a validated agent adapter and profile name, stores state below EnvVault&#39;s private data directory, rejects symlinked state paths, and forces private directory permissions. It never mounts the operator&#39;s normal agent home implicitly.</li><li>The Docker runtime uses bridge networking, a read-only root filesystem, ephemeral HOME and <code>/tmp</code>, dropped capabilities, <code>no-new-privileges</code>, PID, memory, and CPU limits, and a workspace mount. Native mode adds one selected agent-state mount. It does not mount the host home, OS credential store, or Docker socket.</li><li>Docker containers, gateway listeners, and credential leases are cleaned up after normal exit, lifecycle failure, or Ctrl-C.</li><li>The external sandbox plugin accepts only versioned, bounded NDJSON messages, rejects direct credential outputs and duplicate environment mappings, binds connection grants to the supplied sandbox identity, and revokes every lease on <code>close</code>, EOF, or process cancellation.</li></ul><h2 id="residual-risk" tabindex="-1">Residual Risk <a class="header-anchor" href="#residual-risk" aria-label="Permalink to &quot;Residual Risk&quot;">​</a></h2><p>Direct credential mode places the credential value in the child process environment. It improves repository and <code>.env</code> hygiene but does not hide that credential from the launched process.</p><p>Isolated home-file mode likewise does not hide a credential from the child: the child can read every injected file. A forced termination or system failure can leave the private workspace on disk until <code>envvault doctor --repair</code> removes it. The source template contains references rather than secrets and remains unchanged; child modifications to the normalized resolved copy are discarded. Because the isolated home starts empty, a child that needs unrelated files from the real home must be configured explicitly rather than relying on them. The isolation changes home and config environment variables; it does not mount or sandbox the filesystem. A child that independently resolves the OS account home can still access it. The workspace lifetime follows the direct child, so a daemonized descendant does not keep the workspace alive.</p><p>Local proxy tokens are bearer tokens. Anyone who obtains one can use it until it expires, subject to the proxy allowlist.</p><p>The URL-preserving proxy capability is also visible in the sandbox environment and Docker metadata. A sandbox can use it for every attached route until cleanup or expiry. The public CA is not a credential, but installing it lets the trusted broker terminate TLS for configured destinations and observe allowed request metadata and bodies. A client can bypass this cooperative path by ignoring <code>HTTP(S)_PROXY</code> because direct bridge egress remains available. The broker&#39;s private-destination check protects the host-side proxy only; it is not a Docker firewall.</p><p>The Docker sandbox token is likewise visible to the application and in Docker container metadata. The upstream credential is not delivered in brokered mode, but the container retains direct bridge egress and may reach unrelated internet or host services. The temporary host gateway uses a random token and allowlist; it is not an egress firewall. A host crash or forced EnvVault termination may leave a labeled container requiring manual review and removal.</p><p>Native agent auth intentionally gives the container read-write access to an OAuth-bearing agent home. A malicious image or repository command can exfiltrate refresh material through direct egress, log the state, delete it, or persist poisoned configuration for future invocations. A separate profile limits cross-project reuse but does not make the credential secret from the selected sandbox.</p><p>The external sandbox plugin returns bearer capabilities to its trusted control plane. A platform that logs protocol responses, reuses a capability across sandboxes, exposes the plugin pipes to the sandbox, or fails to apply the returned egress restrictions weakens the boundary. Process EOF performs best-effort cleanup, but an uncatchable process or host failure may leave a gateway alive until its short TTL expires.</p>',20)])])}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
