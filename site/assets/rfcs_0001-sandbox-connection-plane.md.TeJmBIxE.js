import{_ as t,o as a,c as i,a2 as o}from"./chunks/framework.DYQ4mM3C.js";const h=JSON.parse('{"title":"RFC 0001: Sandbox connection plane","description":"","frontmatter":{},"headers":[],"relativePath":"rfcs/0001-sandbox-connection-plane.md","filePath":"rfcs/0001-sandbox-connection-plane.md","lastUpdated":null}'),n={name:"rfcs/0001-sandbox-connection-plane.md"};function r(d,e,s,l,c,p){return a(),i("div",null,[...e[0]||(e[0]=[o('<h1 id="rfc-0001-sandbox-connection-plane" tabindex="-1">RFC 0001: Sandbox connection plane <a class="header-anchor" href="#rfc-0001-sandbox-connection-plane" aria-label="Permalink to &quot;RFC 0001: Sandbox connection plane&quot;">​</a></h1><ul><li>Status: Draft</li><li>Date: 2026-08-03</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>EnvVault will evolve from a local secret launcher into a local-first connection broker for commands running in existing sandbox runtimes. In brokered modes, the application or AI agent receives a short-lived connection capability, not the upstream credential. A trusted gateway outside the sandbox authenticates approved connections with credentials obtained from a credential provider.</p><p>This RFC defines the connection domain and its phased integration. The initial EVS-001 slice added only the model; later explicitly selected slices add the compatibility adapter, orchestration, and experimental runtime without changing the stored configuration format or the existing <code>envvault exec</code> contract.</p><h2 id="product-boundary" tabindex="-1">Product boundary <a class="header-anchor" href="#product-boundary" aria-label="Permalink to &quot;Product boundary&quot;">​</a></h2><p>EnvVault will provide three pieces:</p><ol><li>thin adapters for existing sandbox runtimes;</li><li>a trusted, protocol-aware connection gateway outside the sandbox; and</li><li>policy for destinations, protocols, credential sources, lifetimes, and resource limits.</li></ol><p>EnvVault will not implement a hypervisor, container engine, generic DLP system, SQL authorization firewall, dynamic-secret engine, or production PaaS.</p><p>NVIDIA OpenShell is a reference product for gateway, provider, and policy design. OpenSandbox is a separate project and a possible future runtime adapter; this RFC does not integrate either one.</p><h2 id="compatibility" tabindex="-1">Compatibility <a class="header-anchor" href="#compatibility" aria-label="Permalink to &quot;Compatibility&quot;">​</a></h2><p>Existing behavior remains unchanged:</p><ul><li><code>envvault exec</code> may materialize direct <code>envvault://&lt;credential&gt;</code> references.</li><li>home-file resolution remains materialized credential delivery.</li><li>existing provider proxies remain HTTP-only localhost servers.</li><li><code>profile.Profile</code>, <code>issuer.Grant</code>, and <code>internal/runtime/talos</code> retain their current names and responsibilities.</li></ul><p>The compatibility layer translates provider-proxy profiles to HTTP connection policies without changing the stored profile schema or activating the new runtime. It preserves the target base path and provider strategy. Project binding remains on the legacy profile and must be checked before a translated policy is used.</p><p>The translator rejects target URL userinfo, fixed query strings, fragments, and encoded base paths even though legacy validation accepted them. Those values cannot be represented unambiguously by the typed policy and therefore fail closed. Allowed methods and credential references must also round-trip without normalization or reserved-suffix ambiguity. A zero policy connection or byte limit means that the legacy source did not specify that limit; grant issuance must resolve the connection limit to a positive value before creating a capability.</p><h2 id="implementation-status" tabindex="-1">Implementation status <a class="header-anchor" href="#implementation-status" aria-label="Permalink to &quot;Implementation status&quot;">​</a></h2><p>The first compatibility slice now includes the provider-proxy translator, an HTTP <code>ProtocolAdapter</code>, a static keyring credential provider, runtime-neutral sandbox lifecycle orchestration, and an experimental Docker runtime exposed as <code>envvault sandbox run</code>. Existing <code>envvault exec</code> and stored profile formats remain compatible.</p><p>The Docker runtime reports <code>brokered</code>. It deliberately does not claim <code>brokered-enforced</code>: the application uses Docker bridge networking with direct egress, and the container-reachable host gateway is protected by a temporary capability rather than network allowlisting. Enforced topology remains subject to a separate egress RFC and acceptance tests.</p><p>EVS-006 adds a versioned stdio plugin contract for existing agent-sandbox control planes. That contract makes the external sandbox platform the owner of runtime and network enforcement while EnvVault owns connection leases and raw credential handling. See <a href="./0002-external-sandbox-plugin.html">RFC 0002</a>.</p><p>The next experimental slice adds a runtime-neutral outbound broker and Docker egress attachment that preserve original provider URLs through an authenticated HTTP proxy and ephemeral CA. It remains cooperative <code>brokered</code> delivery; direct egress is not blocked. See <a href="./0003-url-preserving-outbound-broker.html">RFC 0003</a>.</p><h2 id="trust-boundary" tabindex="-1">Trust boundary <a class="header-anchor" href="#trust-boundary" aria-label="Permalink to &quot;Trust boundary&quot;">​</a></h2><p>The EnvVault CLI, credential provider, connection gateway, and policy engine are trusted. The sandbox, application, AI agent, generated code, and installed packages are untrusted. A raw credential may exist in the credential provider, trusted gateway memory, and the authenticated upstream request. It must not be placed in the sandbox environment, filesystem, command line, image, grant, audit record, or error output when delivery is <code>proxy</code>.</p><p>A localhost address inside a container or VM refers to the sandbox, not the host. A future runtime must therefore provide either a secretless local relay or a private route to the external gateway. Proxy environment variables alone are not an egress security boundary.</p><h2 id="authentication-dimensions" tabindex="-1">Authentication dimensions <a class="header-anchor" href="#authentication-dimensions" aria-label="Permalink to &quot;Authentication dimensions&quot;">​</a></h2><p>Credential delivery and credential issuance are independent:</p><ul><li><code>delivery: proxy</code> keeps credential material outside the sandbox.</li><li><code>delivery: materialize</code> places credential material in the sandbox and must be an explicit compatibility opt-in for future sandbox commands.</li><li><code>issuance: static</code> reads an existing credential.</li><li><code>issuance: dynamic</code> requests a bounded credential lease from a provider.</li></ul><p>For example, a gateway can proxy a PostgreSQL connection using a dynamic 30-minute OpenBao credential. That is proxy delivery with dynamic issuance.</p><p>The first implementation target is proxy delivery backed by a static keyring credential.</p><h2 id="domain-model" tabindex="-1">Domain model <a class="header-anchor" href="#domain-model" aria-label="Permalink to &quot;Domain model&quot;">​</a></h2><p><code>connection.Policy</code> is the long-lived, repository-safe declaration. It contains a fixed destination, a typed protocol policy, authentication metadata, limits, and metadata-only audit policy.</p><p><code>connection.ProtocolSpec</code> is a discriminated union. Exactly one of its typed protocol policies must be set and it must match <code>ProtocolSpec.Type</code>. Core policy does not use <code>map[string]any</code> for protocol settings.</p><p><code>connection.Grant</code> is non-secret metadata for one short-lived connection capability. The unpredictable opaque capability is stored and compared by the runtime session layer rather than embedded in this model. It is bound to a session, subject, policy revision, protocol, destination, expiry, and limits.</p><p><code>connection.Session</code> contains non-secret runtime counters and revocation state. Credential material is acquired by a protocol adapter only when needed.</p><p>The credential-provider interface exposes material only through a callback so the domain model has no exported raw-secret field. Implementations must redact formatting and errors and clear owned buffers after use.</p><h2 id="protocols" tabindex="-1">Protocols <a class="header-anchor" href="#protocols" aria-label="Permalink to &quot;Protocols&quot;">​</a></h2><p>HTTP is the first adapter. The existing provider proxy is the compatibility baseline: fixed HTTPS or loopback-HTTP destination, bearer injection, exact allowed methods and paths, bounded local token lifetime, project binding, and no secret logging.</p><p>PostgreSQL is the first planned non-HTTP adapter. It will terminate the client protocol, bind the requested database and upstream user to policy, verify upstream TLS, and perform upstream authentication. PostgreSQL authorization remains the responsibility of database roles, schema privileges, RLS, and database-side limits. SQL text will not be used as the primary authorization boundary.</p><p>MySQL, Redis, SSH, and generic TCP have typed placeholders so their future RFCs can extend the union without adding fields to <code>profile.Profile</code>. Generic TCP cannot perform arbitrary application credential injection; proxy delivery is rejected until an explicit protocol-specific authentication strategy such as mTLS is designed.</p><h2 id="security-levels" tabindex="-1">Security levels <a class="header-anchor" href="#security-levels" aria-label="Permalink to &quot;Security levels&quot;">​</a></h2><p>Future sandbox executions will report one of these levels:</p><ul><li><code>materialized-static</code>: a fixed raw credential is placed in the sandbox;</li><li><code>materialized-leased</code>: a short-lived raw credential is placed in the sandbox;</li><li><code>brokered</code>: no raw credential is delivered, but direct egress is not fully blocked; or</li><li><code>brokered-enforced</code>: no raw credential is delivered and default-deny egress is enforced outside the sandbox.</li></ul><p>A runtime must not claim <code>brokered-enforced</code> until integration tests demonstrate that direct IP and DNS access, alternate DNS, metadata endpoints, management interfaces, cross-session capabilities, and expired capabilities fail closed.</p><h2 id="audit" tabindex="-1">Audit <a class="header-anchor" href="#audit" aria-label="Permalink to &quot;Audit&quot;">​</a></h2><p>Audit remains metadata-only. Allowed fields include session and sandbox IDs, runtime, policy name and revision, protocol, destination, security level, timestamps, result, byte counters, connection count, and stable deny codes. Credentials, capabilities, proxy tokens, authorization headers, HTTP bodies, SQL text, SSH payloads, Redis arguments, and full environments are excluded.</p><h2 id="delivery-plan" tabindex="-1">Delivery plan <a class="header-anchor" href="#delivery-plan" aria-label="Permalink to &quot;Delivery plan&quot;">​</a></h2><ol><li>Add this RFC and the typed connection model with no behavior change.</li><li>Translate existing provider-proxy profiles into HTTP policies.</li><li>wrap the existing provider proxy as a protocol adapter.</li><li>add a fake sandbox runtime and lifecycle orchestration tests.</li><li>add an experimental Docker runtime.</li><li>add an external agent-sandbox plugin contract without raw credential delivery.</li><li>integrate one existing sandbox platform and test composed egress policy.</li><li>specify and implement a PostgreSQL adapter.</li><li>expand one runtime, protocol, or credential provider at a time.</li></ol><p>Remote gateways are deferred. Their node identity will be separate from a per-session connection grant. A future RFC will specify SSH-assisted initial enrollment, mTLS authentication, certificate rotation, and revocation.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Deliver the work in the numbered slices above. EVS-001 remains a domain-only, no-behavior-change boundary. <code>envvault sandbox run</code> remains a reference runtime; existing sandbox products should integrate through the external plugin contract. OpenShell-specific packaging, PostgreSQL, configuration migration, Talos integration, and enforced egress remain separate slices.</p>',49)])])}const m=t(n,[["render",r]]);export{h as __pageData,m as default};
