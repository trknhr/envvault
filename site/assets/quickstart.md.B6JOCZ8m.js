import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.DYQ4mM3C.js";const c=JSON.parse('{"title":"Quickstart","description":"","frontmatter":{},"headers":[],"relativePath":"quickstart.md","filePath":"quickstart.md","lastUpdated":1784264311000}'),n={name:"quickstart.md"};function l(h,e,p,o,d,r){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="quickstart" tabindex="-1">Quickstart <a class="header-anchor" href="#quickstart" aria-label="Permalink to &quot;Quickstart&quot;">​</a></h1><p>This page describes the default EnvVault local workflow:</p><ol><li>Store a real credential in the OS credential store.</li><li>Keep only an <code>envvault://&lt;credential&gt;</code> reference in <code>.env</code>.</li><li>Launch the app through <code>envvault exec</code>.</li></ol><p>Canonical public references are:</p><ul><li>Direct credential: <code>envvault://&lt;credential&gt;</code></li><li>Proxy base URL: <code>envvault://&lt;proxy&gt;/base-url</code></li><li>Proxy token: <code>envvault://&lt;proxy&gt;/token</code></li></ul><p>Do not add <code>/value</code> to <code>.env</code> references. <code>/value</code> is only part of internal OS credential store account names used by cleanup tooling and diagnostics.</p><p>Use proxy mode as an advanced option when an SDK accepts a custom base URL and bearer token.</p><h2 id="_1-install-envvault" tabindex="-1">1. Install EnvVault <a class="header-anchor" href="#_1-install-envvault" aria-label="Permalink to &quot;1. Install EnvVault&quot;">​</a></h2><p>Install from the Homebrew tap:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">brew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> trknhr/tap/envvault</span></span></code></pre></div><h2 id="_2-start-the-local-admin-ui" tabindex="-1">2. Start the Local Admin UI <a class="header-anchor" href="#_2-start-the-local-admin-ui" aria-label="Permalink to &quot;2. Start the Local Admin UI&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> admin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span></code></pre></div><p>Open the printed localhost URL. The URL contains a local admin token used by the browser UI when it calls EnvVault APIs for that server run.</p><p>The UI can add credentials and create optional proxies. It does not display stored credential values.</p><p>Use <code>envvault admin status</code> to check the background process and <code>envvault admin stop</code> to stop it. <code>envvault admin serve</code> is available for foreground debugging.</p><p>The CLI commands below are equivalent paths for scripting or repeatable local testing.</p><h2 id="_3-add-a-credential" tabindex="-1">3. Add a Credential <a class="header-anchor" href="#_3-add-a-credential" aria-label="Permalink to &quot;3. Add a Credential&quot;">​</a></h2><p>Store the real credential in the OS credential store:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> credential</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app/dev</span></span></code></pre></div><p>Enter the value at the hidden terminal prompt. EnvVault does not accept the credential as a positional argument, keeping it out of shell history and process arguments.</p><p>For non-interactive scripts, pass the value over stdin explicitly:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">printf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;secret-value\\n&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> credential</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> set</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app/dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --value-stdin</span></span></code></pre></div><p>You can also add or update the credential from the Admin UI.</p><p>To remove it later, use <code>envvault credential delete app/dev</code>. EnvVault refuses when a profile still references the credential. <code>envvault credential delete app/dev --cascade</code> removes both the credential and every dependent profile.</p><p>Inventory potential raw credentials before replacing them with EnvVault references:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> inspect</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> inspect</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ~/.config</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --depth</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --include-medium</span></span></code></pre></div><p>The scan reads current files directly, including ignored and untracked files; it does not inspect Git history or follow symlinks. Output contains only paths, locations, rule identifiers, and confidence—not credential values. Generic API-key candidates are medium confidence and require <code>--include-medium</code>; known provider formats remain high confidence. The command uses bounded parallel file scanning; <code>--workers</code> overrides the automatic worker count. Depth zero is unlimited, while <code>--depth 1</code> includes root files and files one directory below the requested root. Skipped paths are listed only with <code>--verbose</code>; their count is always included. The command does not migrate or delete anything.</p><h2 id="_4-add-the-reference-to-env" tabindex="-1">4. Add the Reference to <code>.env</code> <a class="header-anchor" href="#_4-add-the-reference-to-env" aria-label="Permalink to &quot;4. Add the Reference to \`.env\`&quot;">​</a></h2><p>Use the environment variable name expected by the app, and make the value an EnvVault reference:</p><div class="language-dotenv vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dotenv</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">APP_SECRET</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">envvault://app/dev</span></span></code></pre></div><p>You can keep that line in <code>.env</code>, or skip creating a file and pass it directly at launch:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --env</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> APP_SECRET=envvault://app/dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><h2 id="_5-run-the-app" tabindex="-1">5. Run the App <a class="header-anchor" href="#_5-run-the-app" aria-label="Permalink to &quot;5. Run the App&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --env-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .env</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --env-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .env</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span></span></code></pre></div><p><code>envvault exec</code> reads the <code>.env</code> file, resolves <code>envvault://app/dev</code> from the OS credential store, and launches the child process with <code>APP_SECRET</code> populated.</p><p>This mode intentionally passes the raw credential to the child process environment. It is the most compatible path and works with SDKs that expect a normal API key environment variable.</p><h2 id="_6-advanced-isolated-home-files" tabindex="-1">6. Advanced: Isolated Home Files <a class="header-anchor" href="#_6-advanced-isolated-home-files" aria-label="Permalink to &quot;6. Advanced: Isolated Home Files&quot;">​</a></h2><p>Some tools always read a credential or configuration file from the user&#39;s home directory. Keep a repository-safe source file in the project with only the secret fields represented by EnvVault references. For example, create <code>config/hogehoge.yaml</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">endpoint</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://api.example.com</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">envvault://app/dev</span></span></code></pre></div><p>Then run the tool through EnvVault:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --home-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .hogehoge=config/hogehoge.yaml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> your-command</span></span></code></pre></div><p>The left-hand <code>DEST</code> is a safe relative path inside a private, otherwise empty temporary home. The right-hand <code>SOURCE</code> is resolved relative to the invocation working directory; an absolute source path is also allowed. <code>~</code> is not expanded, so use an already-expanded path such as <code>&quot;$HOME/.hogehoge&quot;</code> when the source really belongs in the normal home:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --home-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;.hogehoge=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/.hogehoge&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> your-command</span></span></code></pre></div><p>A bare path uses the same relative source and destination. Because a single dotfile has no format extension, this example reads <code>./.hogehoge</code> as JSON and writes it to <code>~/.hogehoge</code> under the isolated home:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --home-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .hogehoge</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> your-command</span></span></code></pre></div><p>Supported template formats are selected from the source filename:</p><ul><li><code>.json</code> for JSON</li><li><code>.yaml</code> or <code>.yml</code> for YAML</li><li><code>.toml</code> for TOML</li><li>no extension or a single dotfile name for JSON</li></ul><p>An unknown extension is rejected. EnvVault parses the source, recursively resolves values, and serializes the result at <code>DEST</code>. Only a string value that consists entirely of a direct <code>envvault://&lt;credential&gt;</code> reference is replaced. Keys are never resolved. Partial strings such as <code>&quot;Bearer envvault://app/dev&quot;</code> and proxy <code>/base-url</code> or <code>/token</code> outputs are rejected. A credential remains a string even if its text looks like JSON, YAML, or TOML. YAML anchors, aliases, and merge keys are not supported; mapping keys must be strings.</p><p>The result is normalized output; comments, quoting, whitespace, and key order may change. The source file itself is never modified, and changes made by the child are not written back. Templates are limited to 4 MiB and 128 nesting levels. Duplicate keys, multiple JSON or YAML documents, non-regular source files, source symlinks, and credential values that are not valid UTF-8 are rejected before the child starts.</p><p>Destination paths must remain relative to the isolated home. Absolute destinations, <code>~</code>, <code>..</code>, empty path segments, and drive or volume prefixes are rejected. Repeat <code>--home-file</code> when a command needs more than one file or format:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --home-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .config/example/auth.json=config/auth.json</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --home-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .config/example/settings.toml=config/settings.toml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> your-command</span></span></code></pre></div><p>For a tool whose entire file is one raw credential, the earlier shorthand is still available:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --home-file</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .token=envvault://app/dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> your-command</span></span></code></pre></div><p>Temporary directories are private and resolved files are readable and writable only by the current user.</p><p>The child process receives the isolated home through the platform&#39;s home and config environment variables. EnvVault creates the workspace below its private cache directory and copies no unrelated real-home contents. The child therefore cannot rely on other configuration files being present in the isolated home. The child can read the resolved credential files. EnvVault removes the isolated home when the child exits normally. If EnvVault is forcibly terminated or the system fails, <code>envvault doctor --repair</code> removes stale isolated-home workspaces.</p><p>This mode is intended for foreground tools that honor those environment variables. It is not a filesystem sandbox: a tool that resolves the OS account home independently can still reach the real home, and a daemonized descendant does not extend the isolated home&#39;s lifetime after the direct child exits.</p><h2 id="_7-advanced-api-proxy-mode" tabindex="-1">7. Advanced: API Proxy Mode <a class="header-anchor" href="#_7-advanced-api-proxy-mode" aria-label="Permalink to &quot;7. Advanced: API Proxy Mode&quot;">​</a></h2><p>Use proxy mode when the app or SDK accepts a custom base URL and bearer token, and you do not want to pass the real upstream credential to the child process.</p><p>Add a localhost proxy:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> api-proxy/dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --credential</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app/dev</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --provider</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> generic</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --target</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://api.example.com</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --allow-path</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /v1/messages</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --allow-method</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span></span></code></pre></div><p>The command prints a generic <code>.env</code> snippet:</p><div class="language-dotenv vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dotenv</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ENVVAULT_PROXY_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">envvault://api-proxy/dev/base-url</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ENVVAULT_PROXY_TOKEN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">envvault://api-proxy/dev/token</span></span></code></pre></div><p>The Admin UI shows the same snippet with a copy button for proxies.</p><p>Use the generated right-hand references with the variable names expected by the app:</p><div class="language-dotenv vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dotenv</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">APP_BASE_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">envvault://api-proxy/dev/base-url</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">APP_API_TOKEN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">envvault://api-proxy/dev/token</span></span></code></pre></div><p><code>base-url</code> and <code>token</code> are EnvVault-generated proxy outputs. They are not separate credentials to register.</p><p>At runtime, <code>envvault exec</code> starts a localhost proxy, rewrites <code>APP_BASE_URL</code> to that proxy, and rewrites <code>APP_API_TOKEN</code> to a local-only bearer token. The proxy adds the real upstream credential only for requests matching the allowlist.</p><p>Proxy mode reduces raw-secret exposure, but it also requires separate local environment variables for the provider base URL.</p><h2 id="_8-inspect-health" tabindex="-1">8. Inspect Health <a class="header-anchor" href="#_8-inspect-health" aria-label="Permalink to &quot;8. Inspect Health&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> credential</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> doctor</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --repair</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> reset</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dry-run</span></span></code></pre></div><p><code>doctor</code> reports local state health. <code>doctor --repair</code> removes stale EnvVault runtime locks, temporary files, and isolated-home workspaces before re-checking health. <code>reset --dry-run</code> shows EnvVault-owned files and keyring entries that would be removed.</p><h2 id="_9-generate-shell-completion" tabindex="-1">9. Generate Shell Completion <a class="header-anchor" href="#_9-generate-shell-completion" aria-label="Permalink to &quot;9. Generate Shell Completion&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> completion</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> completion</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> zsh</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> completion</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fish</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">envvault</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> completion</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> powershell</span></span></code></pre></div><p>Write the generated script to the completion location used by your shell.</p><h2 id="_10-install-the-agent-skill" tabindex="-1">10. Install The Agent Skill <a class="header-anchor" href="#_10-install-the-agent-skill" aria-label="Permalink to &quot;10. Install The Agent Skill&quot;">​</a></h2><p>EnvVault includes an agent skill for tools that need to launch commands with EnvVault, configure credentials or proxies, or debug <code>envvault://</code> references.</p><p>Install it from the public repository:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> trknhr/envvault</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --skill</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> envvault</span></span></code></pre></div><p>From a local checkout, use:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> skills</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --skill</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> envvault</span></span></code></pre></div><p>Use the <code>skills</code> CLI options to choose global/project scope or a specific agent, for example <code>-g</code> for global installation or <code>-a &lt;agent&gt;</code>. Restart your agent after installing or updating skills.</p>`,81)])])}const g=s(n,[["render",l]]);export{c as __pageData,g as default};
